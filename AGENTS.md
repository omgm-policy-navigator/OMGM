# AGENTS.md

## 저장소 경계

이 저장소는 「나만 결혼해?」 MVP의 단일 저장소다. 프론트엔드, 백엔드, 검수된 정책 CSV 기준 데이터, 로컬 인프라, 공식 문서를 함께 관리한다.

## 폴더 책임

- `frontend/`: React Web UI와 브라우저 상태. 챗봇 패널, 정책 그래프, 정책 상세, 내 정책, 저장 정책, 알림 상태, 확인 필요 정보 입력, 판정 결과와 정책 근거 표시를 담당한다.
- `backend/`: FastAPI 모듈러 모놀리스. API, REST/SSE 경계, 정책 CSV 로딩과 검증, 대화 오케스트레이션, 질문 엔진, 정책 조회, Rule Engine, RAG, 그래프 Projection, 저장 정책, 알림, 사용자 사실 정보, 판정 결과 관리를 담당한다.
- `backend/data/policy-seed/`: 관리자 검수를 마친 정책, 질문, Rule, 관계, RAG 문서 CSV의 버전 관리 기준본이다.
- `infra/`: Docker Compose, PostgreSQL, pgvector, Ollama, 초기화 스크립트, 로컬 인프라 실행 문서를 담당한다.
- `docs/`: 제품, 아키텍처, 도메인, 데이터, RAG, 보안, 개발 추적, ADR, troubleshooting 기준을 담당한다.
- `scripts/`: 반복 검증용 보조 스크립트만 둔다. README 명령을 숨기기 위한 래퍼로 남용하지 않는다.
- `sample-data/`: 비민감 샘플 데이터만 둔다.

## 아키텍처 규칙

백엔드는 여러 서비스가 아니라 하나의 FastAPI 모듈러 모놀리스다. 기능 책임으로 나누되 `domain/application/infrastructure/interface/repository/service/usecase/adapter/port` 같은 계층을 모든 기능에 반복 생성하지 않는다.

프론트엔드는 FastAPI와 REST API 및 SSE로 통신한다. 현재 WebSocket은 기본 도입하지 않는다.

PostgreSQL은 정형 데이터와 `pgvector` 검색 데이터를 함께 관리한다. 별도 Graph DB나 별도 Vector DB는 현재 도입하지 않는다.

Ollama는 정책 조건 후보 추출 보조, 자연어 질문 이해, 검색 질의 보정, 판정 결과 설명 생성에 사용한다. 최종 자격 상태는 Rule Engine이 구조화된 정책 규칙과 사용자 사실을 비교해 계산한다.

MVP는 별도 정책 수집 파이프라인을 실행하지 않는다. 검수된 CSV 기준본을 백엔드가 읽기 전용으로 로딩하고, 공식 확인 placeholder는 확정 판정에서 제외한다.

## 금지되는 의존성

- 프론트엔드가 PostgreSQL에 직접 접근하지 않는다.
- 프론트엔드가 Ollama를 직접 호출하지 않는다.
- 백엔드 API 라우터에 SQL과 판정 로직을 직접 작성하지 않는다.
- RAG 모듈이 최종 자격 상태를 임의 결정하지 않는다.
- Rule Engine이 자연어 생성 책임을 갖지 않는다.
- API 요청 중 외부 정책 원문을 수집하거나 CSV를 변경하지 않는다.
- `infra/`에 애플리케이션 비즈니스 로직을 넣지 않는다.

## 코드 작성 규칙

입력은 경계에서 검증하고, 대표 오류는 구조화된 응답으로 반환한다. 비밀정보, 토큰, 비밀번호, 암호화 키, 개인정보, 소득·자산 정보는 코드와 로그에 남기지 않는다.

사용하지 않는 인터페이스, 미래 기능용 빈 클래스, 과도한 추상화를 만들지 않는다. 실제 구현이 없는 기능 폴더를 대량 생성하지 않는다.

## 테스트 규칙

기본 검증은 외부 API 키 없이 실행되어야 한다. 백엔드는 설정 로딩, 정책 CSV 무결성, Health Check, 오류 응답, Rule Engine 핵심 규칙을 테스트한다. 프론트엔드는 렌더링, 타입 검사, 빌드를 검증한다.

테스트 데이터에 실제 개인정보나 실제 정책 신청 정보를 사용하지 않는다.

## 문서 갱신 규칙

다음 변경이 있으면 관련 문서를 함께 수정한다.

- 폴더 책임 변경.
- API 계약 변경.
- 데이터 모델 변경.
- 정책 상태 변경.
- RAG 흐름 변경.
- Ollama 모델 변경.
- 정책 수집 흐름 변경.
- 개인정보 저장 범위 변경.
- 인프라 실행 방법 변경.

README는 실행 진입점과 핵심 링크만 유지한다. 상세 설계는 `docs/`에 둔다. ADR은 되돌리기 어려운 결정이 있을 때만 작성하고, troubleshooting은 실제 장애와 해결 과정이 확인된 뒤에만 작성한다.
